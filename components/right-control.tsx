import styles from "./right-control.module.css";
import { useEffect, useRef, useState } from "react";

const RightControlPanel = () => {
  return <div className={styles.body}>Right Control Panel</div>;
};

export default RightControlPanel;
