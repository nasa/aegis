type AMeasurement = {
  notes: string;
  size: number;
  color: string; // specify color options? Or have UI suggest past-used colors?
  shape: string; // ditto with color ^
};

type AObservation = {
  notes: string;
};

type ASample = {
  notes: string;
};
